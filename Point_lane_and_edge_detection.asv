x = zeros(100,1);
x1 = x+1;
im = zeros(100,100);
xx = 60;
xy = 80;
yx = 35;
yy = yx+xy-xx;
for i = 1:100
    if i < xy && i >= xx
        im(i,yx:yy) = x1(xx:xy);
    else
        im(i,:) = x;
    end
end
L_f = imfilter(im
% imshow(im)